$('.slick01').slick({
  // 自動再生。trueで自動再生される。
  autoplay: true,
  // 自動再生で切り替えるを時間
  autoplaySpeed: 10000,
  // 切り替えのアニメーション。ease, linear, ease-in, ease-out, ease-in-out
  cssEase: 'ease-in-out',
  // 左右の次へ、前へボタンを表示するかどうか
  arrows: false
});